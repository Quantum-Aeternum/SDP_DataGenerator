import { NumberMultiplication } from './number-multiplication';

describe('NumberMultiplication', () => {
  it('should create an instance', () => {
    expect(new NumberMultiplication()).toBeTruthy();
  });
});
